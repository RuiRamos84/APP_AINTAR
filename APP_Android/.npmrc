# @tensorflow/tfjs-react-native declara peer-dependencies antigas (tfjs 3.x);
# usamo-lo com tfjs 4.22 (a mesma versão embutida na build nobundle do
# face-api), o que funciona em runtime mas faria o npm recusar a resolução.
legacy-peer-deps=true
